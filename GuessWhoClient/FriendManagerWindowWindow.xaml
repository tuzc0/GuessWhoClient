<Window x:Class="WPFGuessWhoClient.FriendManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Friend Manager"
        Height="600" Width="950"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/PixelTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Center" Margin="0,0,0,8">
            <TextBox x:Name="TxtSearch" Width="360" Height="34" Margin="0,0,8,0"
                     VerticalContentAlignment="Center" FontSize="14"
                     ToolTip="Enter display name to search"/>

            <Button Content="Search" Width="120" Height="34" Margin="0,0,8,0"
                    Style="{StaticResource Btn.Primary}" Click="BtnSearch_Click"/>

            <Button Content="Refresh Requests" Width="160" Height="34"
                    Style="{StaticResource Btn.Primary}" Click="BtnRefreshRequests_Click"/>

            <TextBlock x:Name="LblStatus" Margin="12,0,0,0" VerticalAlignment="Center"
                       Style="{StaticResource Txt.Pixel}" FontSize="14" Foreground="#333"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,8,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="Search Results" Grid.Column="0" FontFamily="Consolas" FontSize="14">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DataGrid x:Name="DgProfiles"
                              Grid.Row="0"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              Margin="4">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding UserId}" Width="60"/>
                            <DataGridTextColumn Header="Display Name" Binding="{Binding DisplayName}" Width="*"/>
                            <DataGridTextColumn Header="Avatar" Binding="{Binding AvatarUrl}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="4">
                        <Button Content="Send Friend Request" Width="180" Height="32"
                                Style="{StaticResource Btn.Primary}"
                                Click="BtnSendRequest_Click"/>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <GroupBox Header="My Friends" Grid.Column="1" FontFamily="Consolas" FontSize="14" Margin="8,0,0,0">
                <Grid>
                    <TextBlock Text="Feature not available in current server version."
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               TextWrapping="Wrap" Foreground="Gray" TextAlignment="Center"/>
                </Grid>
            </GroupBox>
        </Grid>

        <GroupBox Header="Pending Requests Management" Grid.Row="2" FontFamily="Consolas" FontSize="14">
            <Grid Margin="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <DataGrid x:Name="DgRequests"
                          Grid.Column="0"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Req ID" Binding="{Binding FriendRequestId}" Width="80"/>
                        <DataGridTextColumn Header="Requester ID" Binding="{Binding RequesterUserId}" Width="120"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding CreatedAt}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Column="1" Orientation="Vertical" Margin="8,0,0,0" VerticalAlignment="Top">
                    <Button Content="Accept" Width="120" Height="34" Margin="0,0,0,8"
                            Style="{StaticResource Btn.Primary}" Background="#4CAF50" 
                            Click="BtnAccept_Click"/>

                    <Button Content="Reject" Width="120" Height="34" Margin="0,0,0,8"
                            Style="{StaticResource Btn.Primary}" Background="#F44336"
                            Click="BtnReject_Click"/>

                    <Button Content="Cancel (Mine)" Width="120" Height="34"
                            Style="{StaticResource Btn.Primary}" Background="#FF9800"
                            ToolTip="Cancel a request I sent"
                            Click="BtnCancel_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>

    </Grid>
</Window>