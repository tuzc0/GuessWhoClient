<Window x:Class="WPFGuessWhoClient.Presentation.ViewModels.FriendManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Friend Manager" Height="650" Width="1000"
        Background="{StaticResource Brush.Bg}"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/PixelTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="Card.FriendManager" TargetType="GroupBox">
                <Setter Property="Padding" Value="10" />
                <Setter Property="BorderBrush" Value="{StaticResource Brush.AccentDark}" />
                <Setter Property="BorderThickness" Value="2" />
                <Setter Property="Background" Value="{StaticResource Brush.Card}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="GroupBox">
                            <Border BorderThickness="{TemplateBinding BorderThickness}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    Background="{TemplateBinding Background}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <ContentPresenter Grid.Row="0" Margin="5,0,0,5" ContentSource="Header" TextElement.FontWeight="Bold" TextElement.Foreground="{StaticResource Brush.Accent}"/>
                                    <ContentPresenter Grid.Row="1" Margin="{TemplateBinding Padding}"/>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="Btn.Accept" TargetType="Button" BasedOn="{StaticResource Btn.Primary}">
                <Setter Property="Background" Value="#4CAF50" />
                <Setter Property="Foreground" Value="White" />
            </Style>

            <Style x:Key="Btn.Reject" TargetType="Button" BasedOn="{StaticResource Btn.Primary}">
                <Setter Property="Background" Value="#F44336" />
                <Setter Property="Foreground" Value="White" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Border HorizontalAlignment="Center" VerticalAlignment="Center" CornerRadius="16" Background="{StaticResource Brush.Bg}" BorderBrush="{StaticResource Brush.AccentDark}" BorderThickness="3" Padding="20" Width="980" MaxHeight="620">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,16">
                        <TextBox Width="360" Height="34" Margin="0,0,8,0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource {x:Type TextBox}}"/>
                        <Button Content="Search" Width="120" Command="{Binding SearchProfilesCommand}" Style="{StaticResource Btn.Primary}"/>
                        <Button Content="Refresh List" Width="160" Margin="8,0,0,0" Command="{Binding LoadFriendsCommand}" Style="{StaticResource Btn.Primary}"/>
                    </StackPanel>

                    <Grid Grid.Row="1" MinHeight="250">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <GroupBox Header="Search Results" Grid.Column="0" Style="{StaticResource Card.FriendManager}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <DataGrid AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding SearchResults}" SelectedItem="{Binding SelectedProfile}" SelectionMode="Single">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Username" Binding="{Binding DisplayName}" Width="*"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                                <Button Grid.Row="1" Content="Send Friend Request" HorizontalAlignment="Right" Margin="0,4,0,0" Command="{Binding SendFriendRequestCommand}" Style="{StaticResource Btn.Primary}"/>
                            </Grid>
                        </GroupBox>

                        <GroupBox Header="My Friends" Grid.Column="1" Margin="8,0,0,0" Style="{StaticResource Card.FriendManager}">
                            <DataGrid AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding Friends}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Username" Binding="{Binding DisplayName}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>
                    </Grid>

                    <GroupBox Header="Received Friend Requests" Grid.Row="2" Margin="0,8,0,0" Style="{StaticResource Card.FriendManager}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <DataGrid AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding PendingRequests}" SelectedItem="{Binding SelectedPendingRequest}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="From User" Binding="{Binding SenderName}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                <Button Content="Accept" Width="120" Height="34" Command="{Binding AcceptFriendRequestCommand}" Style="{StaticResource Btn.Accept}"/>
                                <Button Content="Reject" Width="120" Height="34" Margin="0,8,0,0" Command="{Binding RejectFriendRequestCommand}" Style="{StaticResource Btn.Reject}"/>
                            </StackPanel>
                        </Grid>
                    </GroupBox>
                </Grid>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>