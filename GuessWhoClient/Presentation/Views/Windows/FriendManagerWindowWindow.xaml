<Window x:Class="WPFGuessWhoClient.Presentation.ViewModels.FriendManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Friend Manager"
        Height="650" Width="1000"
        Background="{StaticResource Brush.Bg}"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/PixelTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="Card.FriendManager" TargetType="GroupBox">
                <Setter Property="Padding" Value="10" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="FontFamily" Value="Consolas" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Foreground" Value="{StaticResource Brush.TextLight}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="GroupBox">
                            <Border BorderThickness="{TemplateBinding BorderThickness}"
                                    BorderBrush="{StaticResource Brush.AccentDark}"
                                    Background="{TemplateBinding Background}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <ContentPresenter Grid.Row="0" Margin="5,0,0,5" ContentSource="Header" RecognizesAccessKey="True" TextElement.FontWeight="Bold" TextElement.Foreground="{StaticResource Brush.Accent}"/>
                                    <ContentPresenter Grid.Row="1" Margin="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="BorderBrush" Value="{StaticResource Brush.AccentDark}" />
                <Setter Property="BorderThickness" Value="2" />
                <Setter Property="Background" Value="{StaticResource Brush.Card}" />
            </Style>

            <Style x:Key="Btn.Accept" TargetType="Button" BasedOn="{StaticResource Btn.Primary}">
                <Setter Property="Background" Value="#4CAF50" />
                <Setter Property="Foreground" Value="White" />
            </Style>

            <Style x:Key="Btn.Reject" TargetType="Button" BasedOn="{StaticResource Btn.Primary}">
                <Setter Property="Background" Value="#F44336" />
                <Setter Property="Foreground" Value="White" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Border HorizontalAlignment="Center" VerticalAlignment="Center" CornerRadius="16" Background="{StaticResource Brush.Bg}" BorderBrush="{StaticResource Brush.AccentDark}" BorderThickness="3" Padding="20" Width="980" MaxHeight="620">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Center" Margin="0,0,0,16">
                        <TextBox x:Name="TxtSearch" Width="360" Height="34" Margin="0,0,8,0"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 VerticalContentAlignment="Center" FontSize="14"
                                 FontFamily="{StaticResource Font.Pixel}"
                                 Foreground="{StaticResource Brush.TextLight}"
                                 Background="{StaticResource Brush.Panel}"/>

                        <Button Content="Search" Width="120" Height="34" Margin="0,0,8,0"
                                Style="{StaticResource Btn.Primary}" 
                                Command="{Binding SearchProfilesCommand}"/>

                        <Button Content="Refresh List" Width="160" Height="34"
                                Style="{StaticResource Btn.Primary}" 
                                Command="{Binding LoadFriendsCommand}"/>
                    </StackPanel>

                    <Grid Grid.Row="1" Margin="0,8,0,8" MinHeight="250">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <GroupBox Header="Search Results" Grid.Column="0" Style="{StaticResource Card.FriendManager}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <DataGrid x:Name="DgProfiles" Grid.Row="0" AutoGenerateColumns="False" IsReadOnly="True" 
                                          ItemsSource="{Binding SearchResults}" 
                                          SelectedItem="{Binding SelectedProfile}"
                                          SelectionMode="Single" Margin="4">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Username" Binding="{Binding DisplayName}" Width="*"/>
                                    </DataGrid.Columns>
                                </DataGrid>

                                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="4">
                                    <Button Content="Send Friend Request" Width="180" Height="32"
                                            Style="{StaticResource Btn.Primary}"
                                            Command="{Binding SendFriendRequestCommand}"/>
                                </StackPanel>
                            </Grid>
                        </GroupBox>

                        <GroupBox Header="My Friends" Grid.Column="1" Margin="8,0,0,0" Style="{StaticResource Card.FriendManager}">
                            <DataGrid x:Name="DgFriends" AutoGenerateColumns="False" IsReadOnly="True" 
                                      ItemsSource="{Binding Friends}" SelectionMode="Single" Margin="4">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Username" Binding="{Binding DisplayName}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>
                    </Grid>

                    <GroupBox Header="Received Friend Requests" Grid.Row="2" Style="{StaticResource Card.FriendManager}" Margin="0,8,0,0">
                        <Grid Margin="4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <DataGrid x:Name="DgReceivedRequests" Grid.Column="0" AutoGenerateColumns="False" IsReadOnly="True" 
                                      ItemsSource="{Binding Friends}" Style="{StaticResource {x:Type DataGrid}}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="From User" Binding="{Binding DisplayName}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>

                            <StackPanel Grid.Column="1" Orientation="Vertical" Margin="8,0,0,0" VerticalAlignment="Top">
                                <Button Content="Accept" Width="120" Height="34" Margin="0,0,0,8"
                                        Style="{StaticResource Btn.Accept}"
                                        Command="{Binding AcceptFriendRequestCommand}"/>
                            </StackPanel>
                        </Grid>
                    </GroupBox>
                </Grid>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>