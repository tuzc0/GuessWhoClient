<UserControl x:Class="GuessWhoClient.Presentation.Views.UserControls.CreateAccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:localization="clr-namespace:GuessWhoClient.Globalization"
             xmlns:behaviors="clr-namespace:GuessWhoClient.Presentation.Behaviors">

    <Grid Background="{StaticResource Brush.Bg}" Margin="0,0,0,-6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                CornerRadius="16"
                Padding="12"
                HorizontalAlignment="Center"
                Margin="0,10,0,10"
                MaxWidth="640">
            <Viewbox Stretch="Uniform" MaxHeight="72">
                <TextBlock Style="{StaticResource Txt.Pixel}"
                           Text="{Binding [AppTitle], Source={x:Static localization:LocalizationProvider.Instance}}"
                           Foreground="{StaticResource Brush.Accent}" />
            </Viewbox>
        </Border>

        <Border Grid.Row="1"
                CornerRadius="16"
                Padding="24"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                MaxWidth="640"
                Width="543"
                Height="334">
            <StackPanel>

                <StackPanel Margin="0,0,0,8">
                    <TextBlock Style="{StaticResource Txt.Pixel}"
                               FontSize="16"
                               Text="{Binding [LblEmail], Source={x:Static localization:LocalizationProvider.Instance}}"
                               Margin="0,0,0,4" />

                    <TextBox x:Name="txtEmail"
                             Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                             MaxLength="254"
                             Height="32"
                             Style="{StaticResource Input.Pixel}" />
                </StackPanel>

                <StackPanel Margin="0,0,0,12">
                    <TextBlock Style="{StaticResource Txt.Pixel}"
                               FontSize="16"
                               Text="{Binding [LblUser], Source={x:Static localization:LocalizationProvider.Instance}}"
                               Margin="0,0,0,4" />

                    <TextBox x:Name="txtDisplayName"
                             Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                             MaxLength="50"
                             Height="32"
                             Style="{StaticResource Input.Pixel}" />
                </StackPanel>

                <StackPanel Margin="0,0,0,12">
                    <TextBlock Style="{StaticResource Txt.Pixel}"
                               FontSize="16"
                               Text="{Binding [LblPassword], Source={x:Static localization:LocalizationProvider.Instance}}"
                               Margin="0,0,0,4" />

                    <PasswordBox x:Name="pwdPassword"
                                 Height="32"
                                 MaxLength="64"
                                 Style="{StaticResource Input.Pixel.Password}"
                                 behaviors:PasswordBoxAssistant.BindPassword="True"
                                 behaviors:PasswordBoxAssistant.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Margin="0,0,0,8">
                    <TextBlock Style="{StaticResource Txt.Pixel}"
                               FontSize="16"
                               Text="{Binding [LblConfirmPassword], Source={x:Static localization:LocalizationProvider.Instance}}"
                               Margin="0,0,0,4" />

                    <PasswordBox x:Name="pwdConfirmPassword"
                                 Height="32"
                                 MaxLength="64"
                                 Style="{StaticResource Input.Pixel.Password}"
                                 behaviors:PasswordBoxAssistant.BindPassword="True"
                                 behaviors:PasswordBoxAssistant.BoundPassword="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <CheckBox x:Name="chkShowPasswords"
                          Style="{StaticResource Chk.Pixel}"
                          Content="{Binding [chkShowPasswords], Source={x:Static localization:LocalizationProvider.Instance}}"
                          Margin="0,4,0,0"
                          HorizontalAlignment="Left"
                          IsChecked="{Binding IsPasswordVisible, Mode=TwoWay}" />

            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <Button x:Name="btnCreateAccount"
                    Content="{Binding [BtnCreateAccount], Source={x:Static localization:LocalizationProvider.Instance}}"
                    Style="{StaticResource Btn.Primary}"
                    Height="48"
                    Width="220"
                    Margin="0,0,0,64"
                    HorizontalAlignment="Center"
                    Command="{Binding CreateAccountCommand}" />

            <Button x:Name="btnBack"
                    Style="{StaticResource Btn.Play.Left}"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Bottom"
                    Margin="30,0,0,64"
                    Command="{Binding BackCommand}" />
        </Grid>
    </Grid>
</UserControl>
