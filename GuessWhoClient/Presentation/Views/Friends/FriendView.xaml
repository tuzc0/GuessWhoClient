<UserControl x:Class="GuessWhoClient.Presentation.Views.Friends.FriendView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Themes/PixelTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <BooleanToVisibilityConverter x:Key="BoolToVisData" />

            <Style x:Key="Card.FriendManager" TargetType="GroupBox">
                <Setter Property="Padding" Value="10" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="FontFamily" Value="Consolas" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Foreground" Value="{StaticResource Brush.TextLight}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="GroupBox">
                            <Border BorderThickness="{TemplateBinding BorderThickness}"
                                    BorderBrush="{StaticResource Brush.AccentDark}"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="8">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <ContentPresenter Grid.Row="0" Margin="5,5,0,5" ContentSource="Header" 
                                                      RecognizesAccessKey="True" TextElement.FontWeight="Bold" 
                                                      TextElement.Foreground="{StaticResource Brush.Accent}"/>
                                    <ContentPresenter Grid.Row="1" Margin="{TemplateBinding Padding}" 
                                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="BorderBrush" Value="{StaticResource Brush.AccentDark}" />
                <Setter Property="BorderThickness" Value="2" />
                <Setter Property="Background" Value="{StaticResource Brush.Card}" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{StaticResource Brush.Bg}">
        <Border HorizontalAlignment="Center" VerticalAlignment="Center" CornerRadius="16" 
                Background="{StaticResource Brush.Bg}" BorderBrush="{StaticResource Brush.AccentDark}" 
                BorderThickness="3" Padding="20" Width="980" MaxHeight="680">

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,16">
                        <TextBox Width="360" Height="34" Margin="0,0,8,0"
                                 FontFamily="Consolas" VerticalContentAlignment="Center"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

                        <Button Content="Search" Width="120" Height="34" Margin="0,0,8,0"
                                Style="{StaticResource Btn.Primary}" 
                                Command="{Binding SearchProfilesCommand}"/>

                        <Button Content="Refresh" Width="120" Height="34"
                                Style="{StaticResource Btn.Primary}" 
                                Command="{Binding LoadFriendsCommand}"/>
                    </StackPanel>

                    <Grid Grid.Row="1" MinHeight="300">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <GroupBox Header="Search Results" Grid.Column="0" Style="{StaticResource Card.FriendManager}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <DataGrid ItemsSource="{Binding SearchResults}" 
                                          SelectedItem="{Binding SelectedProfile}"
                                          AutoGenerateColumns="False" IsReadOnly="True" Height="200"
                                          Background="{StaticResource Brush.Card}">
                                    <DataGrid.Columns>
                                        <DataGridTemplateColumn Header="Avatar" Width="64">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Image Source="{Binding AvatarId}" Width="32" Height="32" Stretch="UniformToFill"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn Header="User" Binding="{Binding DisplayName}" Width="*"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                                <Button Grid.Row="1" Content="Add Friend" HorizontalAlignment="Right"
                                        Style="{StaticResource Btn.Primary}" Margin="0,10,0,0"
                                        Command="{Binding SendFriendRequestCommand}"/>
                            </Grid>
                        </GroupBox>

                        <GroupBox Header="My Friends" Grid.Column="1" Margin="8,0,0,0" Style="{StaticResource Card.FriendManager}">
                            <DataGrid ItemsSource="{Binding Friends}" AutoGenerateColumns="False" IsReadOnly="True"
                                      Background="{StaticResource Brush.Card}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Name" Binding="{Binding DisplayName}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>
                    </Grid>

                    <GroupBox Header="Received Requests" Grid.Row="2" Style="{StaticResource Card.FriendManager}" Margin="0,10,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <DataGrid ItemsSource="{Binding ReceivedRequests}" 
                                      SelectedItem="{Binding SelectedReceivedRequest}"
                                      AutoGenerateColumns="False" IsReadOnly="True" Height="120">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="From User" Binding="{Binding DisplayName}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                                <Button Content="Accept" Style="{StaticResource Btn.Primary}" Width="100" Margin="0,0,0,5"
                                        Command="{Binding AcceptFriendRequestCommand}"/>
                                <Button Content="Reject" Style="{StaticResource Btn.Primary}" Width="100"
                                        Command="{Binding RejectFriendRequestCommand}"/>
                            </StackPanel>
                        </Grid>
                    </GroupBox>

                    <Button Grid.Row="3" Content="Back" HorizontalAlignment="Left" Width="100" Margin="0,15,0,0"
                            Style="{StaticResource Btn.Primary}" Command="{Binding BackCommand}"/>
                </Grid>
            </ScrollViewer>
        </Border>

        <Grid Background="#AA000000" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisData}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="WAITING FOR SERVER..." 
                           FontFamily="Consolas" FontSize="22" 
                           Foreground="{StaticResource Brush.Accent}" 
                           HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <ProgressBar IsIndeterminate="True" Width="250" Height="15" 
                             Foreground="{StaticResource Brush.Accent}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>